# Ingestion avancée - Installation longue (optionnel)
# À installer APRÈS avoir testé l'API avec requirements-core.txt

# LangChain
langchain>=0.3.0
langchain-text-splitters>=0.3.0
langchain-community>=0.3.0

# Document processing (simplifié, sans [all-docs])
unstructured>=0.10.0
python-magic
python-docx
openpyxl
Pillow
markdown

# OCR (optionnel - nécessite tesseract installé sur système)
# pytesseract
# pdf2image
